<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.messager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messager"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:label="@string/app_name"
            android:theme="@style/Theme.Messager"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Adicionando a atividade SetPasswordActivity -->
        <activity
            android:name=".SetPasswordActivity"
            android:label="Definir Senha"
            android:exported="true">
        </activity>

        <!-- Adicionando a atividade LockScreenActivity -->
        <activity
            android:name=".LockScreenActivity"
            android:label="Tela de Bloqueio"
            android:exported="true">
        </activity>
    </application>
</manifest>
